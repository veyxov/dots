# Set a random wallpaper
if [ $# -eq 0 ]; then
    # Update the wallpaper every hour
    watch -n 3600 feh --randomize --bg-fill $WALLPAPERS_LOCATION &>/dev/null &
else
    wall=$1
fi

feh --bg-fill $wall

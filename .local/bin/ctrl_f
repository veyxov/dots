name=$(xwininfo -id $(xdo id) | grep '^xwininfo')
echo "$name"

if [[ $name =~ "Telegram" ]]; then
    herbe "found ! we are on $name" &

    xdotool mousemove 160 50 click --clearmodifiers 1 mousemove restore # Click on telegram search button
else
    herbe "arst" &
    xdotool key ctrl+f
fi

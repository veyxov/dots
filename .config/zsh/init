colorscript random

autoload -Uz compinit
compinit

HISTFILE=~/.histfile
HISTSIZE=1000
SAVEHIST=1000

export KEYTIMEOUT=1 # Kill the lag between switching to VIM mode

bindkey -v

source "./0zinit"
source "./plugins"
source "./alias"
source "./9other"
